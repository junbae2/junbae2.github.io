import React from 'react'
import logo from '../img/logo.svg'

const Header = () => {
  return (
    <header>
      <img src={logo} alt="Funlog Logo" height="40"/>
    </header>
  )
}

export default Header